export enum UserSortEnum {
  NEWEST = 'newest', // sort by newest room first
  OLDEST = 'oldest', // sort by oldest room first

  DATE_OF_BIRTH_ASC = 'dateOfBirth-asc', // from youngest to old
  DATE_OF_BIRTH_DESC = 'dateOfBirth-desk', // from old man to youngest
}
